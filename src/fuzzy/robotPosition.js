const calculateFuzzyValues = (difference) => {

}

export const positionFuzzyfier = (state) => {
    const diffX = Math.abs(state.robot.position.x - state.ball.position.x)
    const diffY = Math.abs(state.robot.position.y - state.ball.position.y)
}

export const positionDefuzzyfier = (fuzzyValue) => {

}

export const positionRules = (fuzzySet) => {

}